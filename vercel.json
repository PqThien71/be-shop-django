{
    "builds": [{
        "src": "BE_shop_django/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.7" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "BE_shop_django/wsgi.py"
        }
    ]
}